<x-app-layout>
    <div class="d-flex flex-wrap justify-content-between align-items-center gap-3 mb-4"><div><div class="small text-primary fw-bold text-uppercase">Administração</div><h1 class="h3 fw-bold mb-1">Usuários</h1><p class="text-muted-custom mb-0">Controle quem pode administrar e corrigir provas.</p></div><a href="{{ route('usuarios.create') }}" class="btn btn-primary"><i class="bi bi-person-plus me-2"></i>Novo usuário</a></div>
    @if(session('success'))<div class="alert alert-success border-0 shadow-sm">{{ session('success') }}</div>@endif
    <div class="card-soft overflow-hidden"><div class="table-responsive"><table class="table align-middle mb-0"><thead class="table-light"><tr><th>Usuário</th><th>E-mail</th><th>Perfil</th><th>Cadastrado em</th><th class="text-end">Ações</th></tr></thead><tbody>@forelse($usuarios as $usuario)<tr><td><div class="d-flex align-items-center gap-2"><span class="avatar">{{ strtoupper(substr($usuario->name, 0, 1)) }}</span><strong>{{ $usuario->name }}</strong></div></td><td>{{ $usuario->email }}</td><td><span class="badge {{ $usuario->isAdmin() ? 'bg-primary' : 'bg-success' }}">{{ $usuario->isAdmin() ? 'Administrador' : 'Professor' }}</span></td><td class="text-muted-custom">{{ $usuario->created_at->format('d/m/Y') }}</td><td class="text-end"><a href="{{ route('usuarios.edit', $usuario) }}" class="btn btn-sm btn-light"><i class="bi bi-pencil"></i></a>@if(auth()->id() !== $usuario->id)<form class="d-inline" method="POST" action="{{ route('usuarios.destroy', $usuario) }}" onsubmit="return confirm('Excluir este usuário?')">@csrf @method('DELETE')<button class="btn btn-sm btn-outline-danger"><i class="bi bi-trash"></i></button></form>@endif</td></tr>@empty<tr><td colspan="5"><div class="empty-state">Nenhum usuário cadastrado.</div></td></tr>@endforelse</tbody></table></div><div class="p-3">{{ $usuarios->links() }}</div></div>
</x-app-layout>
